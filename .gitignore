.DS_Store
.env
/node_modules
.pnpm-store/

# Per-developer IDE discovery dirs (local settings, skill symlinks, MCP config).
# The checked-in guard configs are re-included; everything else stays per-dev.
.cursor/
.claude/*
!.claude/settings.json
.codex/*
!.codex/hooks.json
.agents/

# Test scratch output
/test/.tmp/
